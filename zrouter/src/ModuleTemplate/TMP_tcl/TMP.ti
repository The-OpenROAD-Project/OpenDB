%{
#include "TMP.h"
%}
module TMP
{
	constructor
	(
	);


    	export test_cmd
	(
		in string hello = "world"  [ usage = "says hello" ; ]
	);
    	export test_option
	(
        	in string msg [ usage = "test string msg" ; ]
	);


    	export dump_db
	(
		in string file = "db.dump"  [ usage = "traverses components on to file" ; ]
	);
    	export init
	(
	);
}
