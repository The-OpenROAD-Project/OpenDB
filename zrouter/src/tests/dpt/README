DPT 

DPT is a picture taking facility, which renders shapes to
slices. Later it can map slices to an image, based on colorization
attributes.

see src/dcr/zpt/README for more details on the API


zpt-test.tcl:
	loads /fs/designs/s1, then dumps a bb to dtest and 
	generates a jpeg file

ztest:
	simple tcl program to display an image
