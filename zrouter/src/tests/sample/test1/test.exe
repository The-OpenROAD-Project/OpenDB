#!/bin/bash --noprofile
##Automatically generated test file
##Do not Edit

Z=`which zroute 2> /dev/null`
if [[ -f $Z ]]
then
    ${Z} ${ZARGS} << 'EOF'
    catch {
        source test.tcl
    } _testoutput_
    set _testrc_ $::errorCode
    puts $_testoutput_
    if { $_testrc_ != "NONE" } {exit 1}

EOF

else
    echo "zroute not found in current PATH"
    exit 1
fi
